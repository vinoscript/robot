# 18 - Robot.in_position(x, y)
# The Robot class can be asked to return all robots in a given 
# position (x,y). It should return an array of all the robots 
# since multiple robots could potentially be at position 0,0 (for example)

require_relative "spec_helper"

